package oop.ch09.collectionFramework.v07.map;

public class MapInterfaceEx1 {

	/*
	 * 
	 *   Map 인터페이스
	 *   
	 *     - key-value pair 의 객체를 관리하는 데 필요한 메서드가 정의됨
	 *     
	 *     - key는 중복될 수 없음
	 *     
	 *     - 검색을 위한 자료 구조
	 *     
	 *     - key를 이용하여 값을 저장하거나 검색, 삭제할 때 사용하면 편리함
	 *     
	 *     - 내부적으로 hash 방식으로 구현됨
	 *     
	 *       index = hash(key) // index는 저장 위치
	 *       
	 *     - key가 되는 객체는 객체의 유일성함의 여부를 알기 위해 equals()와 hashCode() 메서드를 재정의함
	 * 
	 */
	
}
