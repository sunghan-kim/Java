package oop.ch09.collectionFramework.v06.set;

public class IteratorEx1 {

	/*
	 * 
	 *   Iterator 로 순회하기
	 *   
	 *     - Collection 의 개체를 순회하는 인터페이스
	 *     
	 *     - Set 인터페이스의 경우, List 인터페이스와 달리 size() 와 같이 요소를 순환할 수 있는 요소가 없어 Iterator 인터페이스 사용
	 *     
	 *     - iterator() 메서드 호출
	 *     
	 *       - 모든 Collection 객체에서 iterator() 메서드를 호출하면 Iterator 객체가 반환된다.
	 *     
	 *         Iterator ir = memberArrayList.iterator();
	 *       
	 *     - Iterator에 선언된 메서드
	 *     
	 *       - boolean hasNext()
	 *       
	 *         - 이후에 요소가 더 있는 지를 체크하는 메서드
	 *         - 요소가 있다면 true를 반환
	 *         
	 *       - E next()
	 *       
	 *         - 다음에 있는 요소를 반환
	 * 
	 */
	
}
