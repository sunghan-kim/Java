package oop.ch08.javaBasicClass.v01.object;

public class ObjectEx1 {

	/*
	 * 
	 *   Object 클래스
	 *   
	 *     - 모든 클래스의 최상위 클래스
	 *     
	 *     - java.lang.Object 클래스
	 *     
	 *     - 모든 클래스는 Object 클래스에서 상속 받음
	 *     - 모든 클래스는 Object 클래스의 메서드를 사용할 수 있음
	 *     - 모든 클래스는 Object 클래스의 일부 메서드를 재정의하여 사용할 수 있음 (final로 지정된 메서드들은 재사용할 수 없다.)
	 *     
	 * 
	 *   Object 클래스의 주요 메서드
	 *   
	 *     - toString()
	 *     - equals()
	 *     - hashCode()
	 *     - clone()
	 *     
	 *     - finalize() : 객체가 메모리에서 해제될 때 gc에서 호출하는 메서드 (리소스 해제 구현)
	 * 
	 */
	
}
