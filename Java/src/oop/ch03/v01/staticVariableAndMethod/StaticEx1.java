package oop.ch03.v01.staticVariableAndMethod;

public class StaticEx1 {

	/*
	 * static 변수
	 * 
	 *   - 여러 인스턴스가 하나의 값을 "공유"할 필요가 있음 (공유 메모리의 필요성)
	 *   - static 이라는 키워드를 갖는 변수는 여러 인스턴스가 그 값을 공유할 수 있다.
	 *   - static 변수는 처음 프로그램이 로드될 때 데이터 영역(상수 영역)에 생성 됨
	 *   - 인스턴스의 생성과 상관없이 사용할 수 있으므로 클래스 이름으로 참조
	 *   
	 *   	Student.serialNum = 100;
	 *   
	 *   - 클래스 변수, 정적 변수라고도 함  
	 *   
	 *   
	 * static 변수와 인스턴스 변수
	 * 
	 *   - 데이터 영역에 위치한 동일한 메모리를 참조
	 * 
	 */

	
}
