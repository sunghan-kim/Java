package oop.ch13.thread.v02.threadUsefulMethod;

public class ThreadPriorityEx1 {

	/*
	 * 
	 * # Thread 우선순위
	 * 
	 *   - Thread.MIN_PRIORITY(=1) ~ Thead.MAX_PRIORITY(=10)
	 *   
	 *   - 디폴트 우선순위 : Thread.NORM_PRIORITY(=5)
	 *   
	 *   - setPriority(int newPriority)
	 *   
	 *   - int get Priority()
	 *   
	 *   - 우선순위가 높은 thread는 C PU를 배분 받을 확률이 높음
	 * 
	 */
	
}
